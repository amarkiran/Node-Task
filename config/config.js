var dbHost = 'localhost';
var dbPort = '27017';
var dbuser = '';
var dbPassword = '';
var dbdatabase = 'admin';

const config = {
    host: dbHost,
    port: dbPort,
    username: dbuser,
    password: dbPassword,
	database: dbdatabase
};

module.exports = config;
